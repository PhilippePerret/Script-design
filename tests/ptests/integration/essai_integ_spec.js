


describe("Un essai de test",[
  context("dans le contexte de la page Projets",[
    it("peut tester cette page", () => {
      log("Je rentre dans le cas it")
      expect(true).to.be.true
      log("J'en ai fini avec le cas it")
    })
  ])
])
